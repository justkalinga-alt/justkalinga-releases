name: Publish exact verified Bridge 0.7.4 dist ZIP

on:
  push:
    branches:
      - recover-jksh-bridge-073
    paths:
      - .github/workflows/publish-jkssb-074-temp-dist.yml
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      -